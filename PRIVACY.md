# RelyLess 隐私政策

生效日期：2026 年 9 月 19 日

本政策说明 RelyLess 浏览器扩展（以下简称"本扩展"）如何处理你的数据。请在使用前阅读。

## 1. 数据控制方与联系方式

发布者：Elazer
联系邮箱：rocky.think.zc@gmail.com

如有隐私相关问题，可通过上述邮箱联系我们。

## 2. 我们如何处理数据

### 2.1 本地优先，不等于完全离线

本扩展的默认定位是"本地优先"：本地领域识别和本机记录不需要把整页上传到我们的自有服务器；本扩展**不提供中转云服务**，也不运营收集你阅读内容的服务器。

但使用 AI 辅助功能时，完成任务所需的文本会发送给你自己选择的模型服务商（例如你配置的 API 端点，或你安装的本机连接器所连接的 ChatGPT 服务）。

**专注阅读**仅在你点击弹窗入口后于当前标签页本地提取并重建当前正文快照；快照只在页面内存中存在，退出、导航或关闭后移除，不新增整页上传、文章存储或页面网址记录，也无需单独配置模型。进入视图本身不调用模型；如果你已开启自动英文辅助或主动请求查词、释义、翻译，仍按下述既有规则发送所需片段。视图顶部的「翻译本页」须单独查看估算并确认，超过预算时再次确认；只按阅读位置发送附近快照正文，退出可撤下译文，但不能撤回已发送且可能计费的请求。选择“只在主动求助时”后，视图也不进行后台分析、预取、标注或记录。不可靠的正文提取会拒绝进入；原图可能请求资源，媒体及未加载图片请回原页查看。
### 2.2 发送给模型服务商的内容

当你使用 AI 辅助功能时，以下内容会发送给你选择的模型服务商：

- **普通辅助**：目标词句及适用的页面标题、章节信息和上下文。专注阅读即使未开启本页自动辅助，按住查词键单击正文英文词仍会主动发送该词和有限上下文；不会因此开始被动扫描。按住查词键单击页面上其他可见英文时，也可明确请求导航以外的链接、侧栏或控件文字的词义；仅发送点中的词及所在文字片段的有限上下文（链接文字最多 200 字符）。输入区、可编辑内容与隐藏文字不参与。导航和控件不会自动扫描或发送。自动预备的文章上下文最多 12,000 字符，较短文章可能整体包含在内。请求同时包含任务指令与结构化输出约束。
- **远程领域识别**（如你选择该模式）：标题和经过长度限制的正文样本会发送到你配置的判定接口（Requesty 或 SiliconFlow System One）。默认的本地领域识别不产生该远程请求。
- **模型路由判定**（如你明确开启模型路由）：目标词句或上下文等最多 2,000 字符的请求摘要会发送到你配置的判定接口，用于判断是否升级服务；判断结果只在本机缓存，无痕窗口不留此缓存。
- **主动翻译选段或导航链接文字**：仅在点击选段操作、按住查词键单击导航链接或使用其右键菜单后发送所选正文或该链接的文字（导航文字最多 200 字符）；正文选段可带有限上下文。导航不会自动扫描或发送，译文直接显示在原标签旁，原文链接不改。
- **本页双语翻译**：弹窗或专注阅读顶部单独确认后才发送附近正文（专注阅读只取当前本地快照）及有限上下文——标题、所在章节各最多 160 字符，相邻正文前后各最多 400 字符；每批最多四项、源文本合计最多 4,000 字符，源文本加上下文最多 12,000 字符。不读取输入框、隐藏正文或视频字幕。
- **阅读摘要**（如你明确开启）：长度受限的阅读样本。
- **个性化分析**（如你进一步开启）：近 30 天内有限的查询、例句、摘要和客观活动统计证据。

**关于「本机模型」通道**：当你把服务提供方选为"本机模型（Gemini Nano）"时，有效的简短单词/短语提示由 Chrome 内置的本机模型在你设备上生成，**不向任何服务器发送文本**；如果本机调用失败或返回无效内容，简短提示也会回落到已配置的就绪 API 服务或已登录订阅。批量辅助、整页翻译、追问等其余请求同样回落；这些回落请求的文本仍按上表发送给相应服务商。没有可用的回落服务时，请求会明确报错而不会静默发送。

请求载荷不主动包含页面网址，但服务商仍可获得正常网络请求元数据，并可能按照其自身条款记录请求、账户和内容。**我们无法承诺第三方或自建 API 服务端不留存数据。** 请在使用前检查所选服务商的隐私与保留政策。

### 2.3 保存在本机的内容

- **API Key 与服务配置**：保存在你浏览器的扩展本机存储中，调用时用于向相应端点认证。
- **个人词档案**：默认开启，用于“记住求助词与支持偏好”。保存短词义标签及其本机模型生成的数字向量（如果模型已预热），不保存原句、标题或来源网址；旧义项缺少向量时仅在需要比对词义且模型已预热时临时计算。你可以在设置中关闭（关闭后不再读取或更新档案）或在“数据与隐私”中清理。
- **阅读记录**（默认关闭，需你明确开启并限定允许的网站）：保存查询词条、长度受限的例句及其译文/释义、阅读摘要与客观统计。客观统计包括按天聚合的活跃时长、可视词数与求助次数；仅在自动辅助模式中采集，不根据滚动或悬停推断完成度或能力。例句最长 1,000 字符（超过则不保存例句），单条译文最长 2,000 字符、释义最长 1,200 字符，自动标注事件中的句子最长 2,000 字符；最多保留 300 条事件、90 天，不含来源网址。可在"阅读记录"或"数据与隐私"中导出或清空。
- **模型用量统计**：模型调用后，在扩展本机存储中按日期、服务名称、模型 ID 和操作类型汇总请求数、失败数以及服务商返回的输入/输出 token 数；没有返回的计数以请求/响应字符数估算并单独标记，不能作为精确账单。最多保留 90 天、400 条汇总记录。不保存请求正文、页面网址或密钥；无痕窗口的模型调用不写入该统计。可在设置的“模型用量”中单独清空，也会随“清空全部阅读数据”删除。关闭阅读记录不会自动删除已有模型用量统计。
- **预备解释与有限上下文缓存**：只保留在当前浏览器会话。
- **术语预填暂存**：在帮助卡片点击“固定译法”时，把该词、领域与参考译法临时写入本机存储，设置页打开并读取后立即删除。
- **帮助与译文缓存（默认仅会话）**：简短词义帮助、选段译文与本页双语翻译的成功结果保存在浏览器会话存储中，关闭浏览器会话后失效；无痕页面不读写这些缓存。缓存键是词条/正文、上下文、任务级别、服务/模型等输入的哈希，不保存来源网址或输入原文明文；**缓存值包含模型生成的提示、中文释义或译文，可能反映敏感内容，哈希也不是匿名化保证**。本页译文另有五分钟、最多 256 项的后台内存缓存。
- **可选跨会话缓存（默认关闭）**：只有在“数据与隐私”中主动开启后，上述成功结果才写入扩展本机持久存储；词义帮助最多 500 条，选段与本页译文合计最多 1,500 条，记录写入后最多保留 30 天，过期条目不再命中并在缓存访问/写入时清理。关闭开关、单独点击“清空帮助与译文缓存”或清空全部阅读数据会删除持久缓存；关闭开关也清理会话缓存。旧版本未经同意留下的持久缓存会在升级初始化时删除。
- **用量与月度预算**：整页翻译开始前会按待译字符数与近 30 天该服务·模型的 tokens/字符比率估算本次消耗（无历史按 4 字符/token 回退），与当月已记用量合计后对照你在“模型服务”中设置的月度 token 预算（0 为不限）；超出时需在弹窗或专注阅读顶部再次确认才继续。估算与用量只含计数，不含原文。

## 3. 需要你明确开启的功能

以下功能默认关闭，只有你明确开启后才运行：

- **阅读记录**：需明确开启并添加允许的网站；关闭采集不会自动删除已有记录。无痕页面不采集阅读记录。
- **阅读摘要**：建立在阅读记录之上，需要额外开启。
- **远程个性化分析**：建立在阅读记录之上，需要额外开启。
- **跨会话保存帮助与译文缓存**：须在“数据与隐私”中单独开启；不受个人词档案或阅读记录开关代替。关闭后删除已有缓存。

## 4. 数据类别说明

本扩展可能处理以下类别的数据：

- **网页内容**：词句、正文样本、标题、章节和上下文，用于模型辅助，相关内容发送至你配置的服务商。
- **认证信息**：你填写的 API Key，保存在本机，用于向相应端点认证。
- **阅读活动**：可选阅读记录及分析处理你允许范围内的阅读、查询和活动证据。

## 5. 数据保留与删除

- 你可以在扩展的"数据与隐私"中导出或清理本机数据。
- 模型用量统计可在设置中单独清空，或随“清空全部阅读数据”一并删除；若本机存储删除失败，界面报告失败，原记录保留，之后可重试。
- 我们不在自有服务器上保留你的阅读内容。
- 已发送给第三方模型服务商的数据，其保留与删除由其自身政策决定，不在我们控制范围内。

## 6. 第三方服务

本扩展允许你连接第三方模型服务商（API 端点或 ChatGPT 订阅连接器）。这些第三方服务的数据处理受其各自隐私政策约束。建议你查看所配置服务商的隐私与保留政策。

## 7. 儿童

本扩展不面向儿童，也不会有意收集儿童的个人信息。

## 8. 政策变更

如本政策发生重大变更，我们会在扩展的新版本说明或本页面进行提示。

## 9. 联系我们

发布者：Elazer
邮箱：rocky.think.zc@gmail.com
